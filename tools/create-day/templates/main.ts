import { readInput } from 'utils';
import { part1, part2 } from './day{{day}}';

const input = readInput('{{year}}-day{{day}}');

console.log(`day {{day}} - part 1: ${part1(input)}`);
console.log(`day {{day}} - part 2: ${part2(input)}`);
